# API base URL (include /api)
# Dev: leave unset (uses /api via Vite proxy)
# Production: full backend URL + /api, e.g. https://pharmacy-pilot-api.onrender.com/api
# VITE_API_URL=https://your-backend.onrender.com/api
